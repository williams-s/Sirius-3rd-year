version: '3.8'

services:
  bridge-messaging:
    container_name: sirius-bridge
    build: .
    network_mode: host
    environment:
      - KAFKA_BOOTSTRAP=192.168.0.50:9092
      - MQTT_BROKER=192.168.0.60
      - MQTT_PORT=1883