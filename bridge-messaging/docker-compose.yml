version: '3.8'

services:
  bridge-messaging:
    container_name: sirius-bridge
    build: .
    environment:
      - KAFKA_BOOTSTRAP= 172.31.125.112:9092
      - MQTT_BROKER=172.31.249.162
      - MQTT_PORT=1883